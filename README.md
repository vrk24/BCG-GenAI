1)Functionality: Explain that it uses an intent-matching keyword approach to retrieve pre-calculated financial metrics.

2)Predefined Queries: List the exact queries the bot understands (e.g., "What is Apple's revenue?").

3)Limitations: Be transparent—note that it doesn't support complex sentence structures, handle typos, or perform real-time math, as it relies on a static dictionary.